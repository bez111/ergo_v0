import type { Viewport } from 'next'

export const viewport: Viewport = {
  themeColor: '#f97316', // orange-500
  width: 'device-width',
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
} 