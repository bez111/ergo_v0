import { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { ErgoScriptCypherpunkClient } from './ErgoScriptCypherpunkClient';
import { infographics } from '@/data/infographics';
import { siteConfig } from '@/config/site-config';

interface Props {
  params: { locale: string };
}

const SLUG = 'ergoscript-cypherpunk-code-of-freedom';

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    return {
      title: 'Infographic Not Found',
      description: 'The requested infographic could not be found.',
    };
  }

  const origin = siteConfig.siteUrl;
  const url = `${origin}/infographics/${infographic.slug}`;

  const title =
    infographic.seoTitle ||
    'ErgoScript: Cypherpunk Smart Contracts, eUTXO & ZK Privacy';
  const description =
    infographic.seoDescription ||
    'Explore how ErgoScript combines the eUTXO model, strict design principles and Sigma Protocols to deliver cypherpunk smart contracts, zk privacy and decentralized apps on Ergo.';

  return {
    title,
    description,
    keywords: infographic.tags,
    alternates: { canonical: url },
    openGraph: {
      type: 'article',
      url,
      siteName: 'Ergo Platform',
      title: 'ErgoScript: The Cypherpunk Code of Freedom',
      description:
        'Infographic breaking down ErgoScript’s eUTXO foundation, deterministic design, Sigma Protocols zk-privacy and how they enable DEXes, stablecoins and DAOs as weapons of decentralization.',
      images: [
        {
          url: `${origin}${infographic.fullImageUrl}`,
          width: 1280,
          height: 670,
          alt: infographic.imageAlt,
        },
      ],
      locale: 'en_US',
    },
    twitter: {
      card: 'summary_large_image',
      title,
      description,
      images: [`${origin}${infographic.fullImageUrl}`],
      site: siteConfig.twitterHandle,
      creator: siteConfig.twitterHandle,
    },
    robots: { index: true, follow: true },
  };
}

export default function ErgoScriptCypherpunkPage({ params }: Props) {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    notFound();
  }

  const structuredData = {
    '@context': 'https://schema.org',
    '@type': 'ImageObject',
    name: infographic.title,
    description: infographic.shortDescription,
    url: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    contentUrl: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    thumbnailUrl: `${siteConfig.siteUrl}${infographic.previewImageUrl}`,
    datePublished: infographic.publishDate,
    author: {
      '@type': 'Organization',
      name: 'Ergo Platform',
    },
    publisher: {
      '@type': 'Organization',
      name: 'Ergo Platform',
      logo: {
        '@type': 'ImageObject',
        url: `${siteConfig.siteUrl}/logo.png`,
      },
    },
    keywords: infographic.tags.join(', '),
    about: [
      {
        '@type': 'Thing',
        name: 'ErgoScript',
      },
      {
        '@type': 'Thing',
        name: 'eUTXO smart contracts',
      },
      {
        '@type': 'Thing',
        name: 'Sigma Protocols',
      },
      {
        '@type': 'Thing',
        name: 'Zero-knowledge privacy',
      },
    ],
  };

  return (
    <>
      <script
        type="application/ld+json"
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML={{
          __html: JSON.stringify(structuredData),
        }}
      />
      <ErgoScriptCypherpunkClient infographic={infographic} />
    </>
  );
}


