import { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { MoneyWithoutMastersClient } from './MoneyWithoutMastersClient';
import { infographics } from '@/data/infographics';
import { siteConfig } from '@/config/site-config';

interface Props {
  params: { locale: string };
}

const SLUG = 'money-without-masters-ergo-vs-banks-and-vc-crypto';

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    return {
      title: 'Infographic Not Found',
      description: 'The requested infographic could not be found.',
    };
  }

  const origin = siteConfig.siteUrl;
  const url = `${origin}/infographics/${infographic.slug}`;

  const title =
    infographic.seoTitle ||
    'Money Without Masters: Ergo vs Traditional Banking & VC Crypto';
  const description =
    infographic.seoDescription ||
    'Compare traditional banking and VC/ICO crypto to the Ergo blockchain and see how fair-launched PoW, eUTXO and cypherpunk design aim to create truly sovereign, censorship-resistant money.';

  return {
    title,
    description,
    keywords: infographic.tags,
    alternates: { canonical: url },
    openGraph: {
      type: 'article',
      url,
      siteName: 'Ergo Platform',
      title: 'Money Without Masters: Open, Censorship-Resistant Finance on Ergo',
      description:
        'Infographic contrasting traditional banking and VC/ICO-style crypto with Ergo’s fair-launched PoW, eUTXO smart contracts, programmable privacy and community-driven ethos.',
      images: [
        {
          url: `${origin}${infographic.fullImageUrl}`,
          width: 1280,
          height: 670,
          alt: infographic.imageAlt,
        },
      ],
      locale: 'en_US',
    },
    twitter: {
      card: 'summary_large_image',
      title,
      description,
      images: [`${origin}${infographic.fullImageUrl}`],
      site: siteConfig.twitterHandle,
      creator: siteConfig.twitterHandle,
    },
    robots: { index: true, follow: true },
  };
}

export default function MoneyWithoutMastersPage({ params }: Props) {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    notFound();
  }

  const structuredData = {
    '@context': 'https://schema.org',
    '@type': 'ImageObject',
    name: infographic.title,
    description: infographic.shortDescription,
    url: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    contentUrl: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    thumbnailUrl: `${siteConfig.siteUrl}${infographic.previewImageUrl}`,
    datePublished: infographic.publishDate,
    author: {
      '@type': 'Organization',
      name: 'Ergo Platform',
    },
    publisher: {
      '@type': 'Organization',
      name: 'Ergo Platform',
      logo: {
        '@type': 'ImageObject',
        url: `${siteConfig.siteUrl}/logo.png`,
      },
    },
    keywords: infographic.tags.join(', '),
    about: [
      {
        '@type': 'Thing',
        name: 'Ergo philosophy and ethos',
      },
      {
        '@type': 'Thing',
        name: 'Financial sovereignty',
      },
      {
        '@type': 'Thing',
        name: 'VC and ICO crypto',
      },
      {
        '@type': 'Thing',
        name: 'Cypherpunk ethos',
      },
    ],
  };

  return (
    <>
      <script
        type="application/ld+json"
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML={{
          __html: JSON.stringify(structuredData),
        }}
      />
      <MoneyWithoutMastersClient infographic={infographic} />
    </>
  );
}



