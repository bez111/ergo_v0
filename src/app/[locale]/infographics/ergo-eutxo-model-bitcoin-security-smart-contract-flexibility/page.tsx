import { Metadata } from 'next';
import { notFound } from 'next/navigation';
import { ErgoEutxoModelClient } from './ErgoEutxoModelClient';
import { infographics } from '@/data/infographics';
import { siteConfig } from '@/config/site-config';

interface Props {
  params: { locale: string };
}

const SLUG = 'ergo-eutxo-model-bitcoin-security-smart-contract-flexibility';

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    return {
      title: 'Infographic Not Found',
      description: 'The requested infographic could not be found.',
    };
  }

  const origin = siteConfig.siteUrl;
  const url = `${origin}/infographics/${infographic.slug}`;

  const title =
    infographic.seoTitle ||
    "Ergo eUTXO Model: Bitcoin Security with Smart Contract Flexibility";
  const description =
    infographic.seoDescription ||
    "Learn how Ergo’s eUTXO model builds on Bitcoin’s UTXO security while adding smart contract flexibility, parallelism, privacy and stronger assurances than the account model.";

  return {
    title,
    description,
    keywords: infographic.tags,
    alternates: { canonical: url },
    openGraph: {
      type: 'article',
      url,
      siteName: 'Ergo Platform',
      title: "Ergo’s eUTXO Model: Bitcoin’s Security Meets Smart Contract Flexibility",
      description:
        "Infographic explaining Ergo’s extended UTXO upgrade, its box concept, programmable validators, key benefits and how eUTXO compares to the traditional account model.",
      images: [
        {
          url: `${origin}${infographic.fullImageUrl}`,
          width: 1280,
          height: 670,
          alt: infographic.imageAlt,
        },
      ],
      locale: 'en_US',
    },
    twitter: {
      card: 'summary_large_image',
      title,
      description,
      images: [`${origin}${infographic.fullImageUrl}`],
      site: siteConfig.twitterHandle,
      creator: siteConfig.twitterHandle,
    },
    robots: { index: true, follow: true },
  };
}

export default function ErgoEutxoModelPage({ params }: Props) {
  const infographic = infographics.find((item) => item.slug === SLUG);

  if (!infographic) {
    notFound();
  }

  const structuredData = {
    '@context': 'https://schema.org',
    '@type': 'ImageObject',
    name: infographic.title,
    description: infographic.shortDescription,
    url: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    contentUrl: `${siteConfig.siteUrl}${infographic.fullImageUrl}`,
    thumbnailUrl: `${siteConfig.siteUrl}${infographic.previewImageUrl}`,
    datePublished: infographic.publishDate,
    author: {
      '@type': 'Organization',
      name: 'Ergo Platform',
    },
    publisher: {
      '@type': 'Organization',
      name: 'Ergo Platform',
      logo: {
        '@type': 'ImageObject',
        url: `${siteConfig.siteUrl}/logo.png`,
      },
    },
    keywords: infographic.tags.join(', '),
    about: [
      {
        '@type': 'Thing',
        name: 'Ergo Blockchain',
      },
      {
        '@type': 'Thing',
        name: 'eUTXO model',
      },
      {
        '@type': 'Thing',
        name: 'Smart contracts',
      },
      {
        '@type': 'Thing',
        name: 'Sigma Protocols',
      },
    ],
  };

  return (
    <>
      <script
        type="application/ld+json"
        // eslint-disable-next-line react/no-danger
        dangerouslySetInnerHTML={{
          __html: JSON.stringify(structuredData),
        }}
      />
      <ErgoEutxoModelClient infographic={infographic} />
    </>
  );
}


